package com.MediUnivers.service.domain;

/** Communication & Notification Engine spec §4 — Phase 1 channels (WhatsApp/Push are Phase 2). */
public enum NotificationChannel {
    EMAIL, SMS, WHATSAPP, IN_APP
}
