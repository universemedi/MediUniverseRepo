package com.MediUnivers.service.domain;

/** Who a WebsiteTemplate is for — MediUnivers' own site, or a tenant organization's public site (req #9). */
public enum TemplateAudience {
    PLATFORM, ORGANIZATION
}
