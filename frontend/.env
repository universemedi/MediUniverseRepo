# URL of the MediUnivers Spring Boot backend (see /service).
# The backend's mediunivers.frontend-base-url must match where THIS app is served from,
# and its registered OAuth2 redirect URI must be <this app's origin>/oauth/callback.
VITE_API_BASE_URL=http://localhost:8080
