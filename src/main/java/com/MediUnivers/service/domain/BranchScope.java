package com.MediUnivers.service.domain;

/** How a user's branch access is granted — every branch now or org-wide, vs a hand-picked subset. */
public enum BranchScope {
    ALL_BRANCHES, SELECTED_BRANCHES
}
